{% extends "template.html.twig" %}

{% block body %}
<h1>Info sur la seance </h1>
<table class="table">
    <tr>
        <th>ID</th>
        <th>Enseignant</th>
        <th>Titre</th>
        <th>Début séance</th>
        <th>Fin séance</th>
    </tr>
<tr>
    <td>
        {{seance_cour.id}}
    </td>
    <td>
        {{seance_cour.enseignant}}
    </td>
    <td>
        {{seance_cour.title}}
    </td>
    <td>
        {{ seance_cour.startdateAt ? seance_cour.startdateAt|date('Y-m-d H:i:s') : '' }}
    </td>
    <td>
        {{ seance_cour.EnddateAt ? seance_cour.startdateAt|date('Y-m-d H:i:s') : '' }}
    </td>
</tr>
</table>

<h1>Listes des candidats</h1>

<table class="table">
    <thead>
    <tr>
        <th>ID</th>
        <th>CIN</th>
        <th>Nom</th>
        <th>Affecter une abscence</th>
        <th>Abscence déja affecter</th>
    </tr>
    </thead>
    
    <tbody>
       
        {% for candidat in seance_cour.candidat %}
      
        <tr>
            <td>{{ candidat.id }}</td>
            <td>{{ candidat.cin }}</td>
            <td>{{ candidat.firstName }}</td>
            <td>
<a href="{{ path('abscence_candidat_seance', {'id': candidat.id}) }}"> <i class="far fa-plus-square" style="margin-left: 50px;"></i></a>



       {# <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#exampleModal" data-whatever="{{ candidt.id }}">
         <i class="glyphicon glyphicon-info-sign"></i>
         </button>#}

            </td>
            <td>
                test
            </td>
        </tr>
        {% else %}
        <tr>
            <td colspan="3">Aucun enregistrement trouvé</td>
        </tr>
        {% endfor %}
    </tbody>

</table>
   {# {% include "seance_cours/affecter_abscence.html.twig" %} #}
 
<a href="{{ path('seance_cours_index') }}"><button type="button" class="btn btn-outline-warning">Retour à la liste</button></a>
{% endblock %}